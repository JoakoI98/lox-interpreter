{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/target/debug/codecrafters-interpreter",
            "args": ["tokenize", "test.txt"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build"
        }
    ]
} 